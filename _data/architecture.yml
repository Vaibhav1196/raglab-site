# ──────────────────────────────────────────────
# Architecture diagram — your stack explained
# ──────────────────────────────────────────────
# Edit the ascii_art block to update the diagram
# Use HTML span classes for colors:
#   hl = green, hl2 = amber, hl3 = cyan
# ──────────────────────────────────────────────

section_comment: "// the stack — updated as I experiment"
section_title: "how it works"

# This is rendered as a <pre> block, so spacing matters
ascii_art: |
    YOU                  raglab_ site              HuggingFace Spaces
    ───                  ──────────────              ────────────────────

    visit site    →    GitHub Pages (free)
    click demo    →    embedded link         →    Gradio / Streamlit app
    try query     →                          →    runs RAG pipeline live
    see results   ←    ← ← ← ← ← ← ← ← ← ←    returns retrieved chunks
                                                   + generated answer

    THE REPO
    ────────
    raglab/
    ├── experiments/          ← each experiment = a folder
    │   ├── chunking-comparison/
    │   ├── embedding-showdown/
    │   └── reranker-test/
    ├── demos/                ← HF Spaces app code
    │   ├── chunking-app/
    │   └── embedding-app/
    ├── docs/                 ← write-ups, findings, notes
    ├── site/                 ← this landing page
    └── README.md
